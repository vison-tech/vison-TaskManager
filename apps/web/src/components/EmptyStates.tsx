import { FolderKanban, Plus, Sparkles } from 'lucide-react';
import { useLocale } from '../lib/locale';

export function EmptyProject({ onCreate }: { onCreate: () => void }) { const { text } = useLocale(); return <div className="empty-state"><div className="empty-icon"><FolderKanban size={24} /></div><h2>{text('创建你的第一个项目', 'Create your first project')}</h2><p>{text('项目用于隔离任务、工作区和 Agent 会话。', 'Projects keep tasks, workspaces, and agent sessions together.')}</p><button className="button primary" onClick={onCreate}><Plus size={16} />{text('新建项目', 'New project')}</button></div>; }
export function EmptyBoard({ onCreate }: { onCreate: () => void }) { const { text } = useLocale(); return <div className="empty-state"><div className="empty-icon warm"><Sparkles size={24} /></div><h2>{text('这个项目还没有任务', 'This project has no tasks yet')}</h2><p>{text('从一个小任务开始，把进度集中在同一个地方。', 'Start with a small task and keep the work in one place.')}</p><button className="button primary" onClick={onCreate}><Plus size={16} />{text('创建第一个任务', 'Create the first task')}</button></div>; }
